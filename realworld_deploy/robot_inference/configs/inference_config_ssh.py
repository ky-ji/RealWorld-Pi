#!/usr/bin/env python3
"""
GR00T 推理客户端配置 - SSH 隧道模式

使用场景:
    推理服务器在远程 GPU 机器上（如云服务器），
    需要通过 SSH 隧道连接
"""

# =============================================================================
# SSH 隧道配置
# =============================================================================

SSH_HOST = "115.190.134.186"  # GPU 服务器地址
SSH_USER = "jikangye"             # SSH 用户名
SSH_PORT = 22                     # SSH 端口
SSH_KEY_NAME = "id_server"        # SSH 密钥文件名 (放在 keys/ 目录)

# 服务器端口配置
SERVER_PORT = 5555                # 远程 GR00T 服务器端口
LOCAL_PORT = 8007                 # 本地隧道端口 (SSH -L 的本地端口)

# =============================================================================
# GR00T 策略服务器配置
# =============================================================================

# 客户端超时配置 (毫秒)
TIMEOUT_MS = 15000

# API Token
API_TOKEN = None

# =============================================================================
# 机器人配置 (Franka Panda via Polymetis)
# =============================================================================

# 注意：这里的 ROBOT_IP 指的是 **Polymetis gRPC 服务所在机器** 的地址，
# 不是 Franka 机械臂本体的 FCI/内网 IP。
# 你的 `control/set_joint_positions.py` 使用 `localhost` 可正常运动，
# 说明 Polymetis 服务运行在本机，因此这里应当填 `localhost`。
ROBOT_IP = "localhost"
ROBOT_PORT = 50051
GRIPPER_PORT = 50052

# =============================================================================
# 相机配置
# =============================================================================

CAMERA_TYPE = "realsense"
CAMERA_INDEX = 0
CAMERA_SERIAL_NUMBER = None
CAMERA_RESOLUTION = (640, 480)
CAMERA_FREQ = 30.0
IMAGE_QUALITY = 85
ENABLE_DEPTH = False

# 多相机配置文件路径 (相对于 robot_inference 目录)
# 如果设置了此路径，将使用配置文件中的相机设置，忽略上面的单相机配置
CAMERA_CONFIG_PATH = "control/cameras/camera_config.json"

# =============================================================================
# 推理配置
# =============================================================================

INFERENCE_FREQ = 10.0
N_OBS_STEPS = 1  # 与模型配置 delta_indices=[0] 对应，只需要1帧
STEPS_PER_INFERENCE = 8
ACTION_SCALE = 1.0

# =============================================================================
# Franka 阻抗控制参数
# =============================================================================

CARTESIAN_KX = [300.0, 300.0, 300.0, 30.0, 30.0, 30.0]
CARTESIAN_KXD = [30.0, 30.0, 30.0, 3.0, 3.0, 3.0]

# =============================================================================
# 任务配置
# =============================================================================

DEFAULT_TASK_INSTRUCTION = "Pick up the pineapple bun from the assembly line and place it on the plate"

# =============================================================================
# 网络配置
# =============================================================================

SOCKET_TIMEOUT = 10.0
BUFFER_SIZE = 65536
ENCODING = 'utf-8'

